# Session log - 2026-06-02

## Context

Continuing from 2026-06-01. PR 76 (full light mode) has merged. Starting item 1 of the backlog: WebSocket reconnect with exponential backoff.

---

## Changes

### 1. WebSocket reconnect hook and TelemetryChart wiring (PR 77)

**Files changed:**

- `frontend/hooks/useWebSocketTelemetry.ts` - new hook, exponential backoff reconnect (1/2/4/8/16s, max 5 attempts), no retry on 1008 auth failure
- `frontend/components/TelemetryChart.tsx` - wired to use WS hook for live pushes, deduplicates by id

### 2. Alert rule evaluation for all v2 sensors (PR 79)

**Files changed:**

- `backend/app/routes/telemetry.py` - replace 6-field hardcoded reading dict with `payload.model_dump(exclude={'device_id'})`
- `backend/tests/test_telemetry.py` - add `test_ingest_v2_sensors_stored`

### 3. ML retrain endpoint POST /api/v1/ml/retrain (PR 80)

**Files changed:**

- `backend/app/routes/ml.py` - new POST /retrain endpoint (admin only, 202, 1-hour cooldown, background task)
- `backend/app/services/ml_service.py` - add FEATURE_COLS, reload_model(), refactor score_reading to use FEATURE_COLS
- `backend/tests/test_ml.py` - add test_retrain_requires_admin, test_retrain_accepted_then_cooldown

### 4. Live sensor grid auto-update on device detail (PR 81)

**Files changed:**

- `frontend/app/devices/[id]/page.tsx` - replace one-time latest fetch with useTelemetry poll; pass liveReadings[0] to SensorGrid

### 5. git hook execute bit fix (PR 78)

**Files changed:**

- `.githooks/commit-msg` - mode 644 -> 755
- `.githooks/prepare-commit-msg` - mode 644 -> 755

### 6. alerts.resolved ORM column String -> Boolean (PR 82)

**Files changed:**

- `backend/app/models/alert.py` - Column(String) -> Column(Boolean)
- `backend/app/routes/alerts.py` - remove str(resolved) cast in filter

### 7. Ticket creation from alert banner (PR 83)

**Files changed:**

- `frontend/components/AlertBanner.tsx` - client component, Create Ticket button, modal with prefilled TicketForm

### 8. Pagination on tickets and devices (PR 84 - in progress)

**Files changed:**

- `backend/app/routes/tickets.py` - add skip/limit params
- `backend/app/routes/devices.py` - add skip/limit params
- `frontend/app/tickets/page.tsx` - page state + Prev/Next controls
- `frontend/app/devices/page.tsx` - page state + Prev/Next controls
