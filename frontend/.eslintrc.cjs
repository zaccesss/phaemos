// ESLint configuration for the Next.js frontend.
// Keeping this file in the repo ensures `next lint` runs non-interactively in CI.
module.exports = {
  extends: ["next/core-web-vitals", "next/typescript"],
};
