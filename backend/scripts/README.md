# Scripts

One-off utility scripts, not part of the application runtime.

| File | Purpose |
|---|---|
| quick_api_smoke.py | Smoke test - hits key endpoints and prints pass/fail. Run after deployment to verify the live API is healthy. |
